apiVersion: v1
kind: Service
metadata:
  name: todo-app-service
  namespace: mateapp
  labels:
    app: todo-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: todo-app
